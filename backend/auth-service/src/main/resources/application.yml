server:
  port: 8081

spring:
  application:
    name: auth-service
  profiles:
    # Local runs default to dev; containers set SPRING_PROFILES_ACTIVE=docker.
    default: dev
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  mail:
    host: ${SPRING_MAIL_HOST:localhost}
    port: ${SPRING_MAIL_PORT:1025}

# Mail transport seam (ADR-001): smtp = Mailpit/real provider via spring.mail.*;
# log = record intent only, set by context-booting integration tests.
# app.mail.from is deliberately NOT spring.mail.from: Boot's MailProperties has
# no from field — the adapter stamps it on every message itself.
app:
  mail:
    transport: smtp
    from: ${SPRING_MAIL_FROM:noreply@foleybooks.com}
    # AU-09 (ADR-001/D-02): total delivery attempts per message (initial +
    # scheduled retries) and the fixed backoff between them. Exhaustion logs
    # an error; the request flow is never affected and FR-02 resend recovers.
    retry:
      max-attempts: 3
      delay: 1s
  # D-01: confirmation emails link to <base>/verify-email?token=… — the same
  # FRONTEND_BASE_URL the gateway uses for CORS (.env.example "Frontend").
  frontend:
    base-url: ${FRONTEND_BASE_URL:http://localhost:5173}
  # D-03 (AGENTS.md §5): RS256 access tokens. Blank PEM ⇒ dev auto-generates an
  # ephemeral pair at startup (C19 — no committed keys); real deployments supply
  # JWT_PRIVATE_KEY/JWT_PUBLIC_KEY (.env.example "JWT").
  jwt:
    issuer: ${JWT_ISSUER:http://localhost:8081}
    access-token-ttl: 15m
    private-key-pem: ${JWT_PRIVATE_KEY:}
    public-key-pem: ${JWT_PUBLIC_KEY:}

eureka:
  client:
    service-url:
      # Registry credentials embedded per ADR-006; no committed defaults.
      defaultZone: http://${EUREKA_USERNAME}:${EUREKA_PASSWORD}@${EUREKA_HOSTNAME:localhost}:8761/eureka/

logging:
  pattern:
    # MDC traceId set by TraceIdFilter — the same id every ProblemDetail carries (NFR-06, D-15).
    level: "%5p [${spring.application.name:},%X{traceId:-}]"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: never
      probes:
        enabled: true

# Swagger UI is a dev-profile-only surface (AGENTS.md §6).
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    # Host-run dev: expects the database env trio from .env (C19 — no committed password).
    url: jdbc:postgresql://localhost:5432/${AUTH_DB_NAME:auth_db}
    username: ${AUTH_DB_USER:auth_user}
    password: ${AUTH_DB_PASSWORD}

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    # docker-compose overrides the full SPRING_DATASOURCE_* trio; these are the
    # compose-network fallbacks (auth-db answers on :5432 inside the network, C27).
    # No committed password (C19): startup fails fast when the env is missing.
    url: jdbc:postgresql://auth-db:5432/${AUTH_DB_NAME:auth_db}
    username: ${AUTH_DB_USER:auth_user}
    password: ${AUTH_DB_PASSWORD}

app:
  jwt:
    # Inside the compose network auth-service answers as auth-service:8081 (C27);
    # docker-compose also forwards a JWT_ISSUER override (.env.example "JWT").
    issuer: ${JWT_ISSUER:http://auth-service:8081}
